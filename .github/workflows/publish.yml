name: Publish
on:
  release:
    types: [published]

permissions:
  id-token: write
  contents: read

jobs:
  publish:
    uses: ExtraGalaxies/finsys-core/.github/workflows/npm-package-publish.yml@0e5ea9ad63ca61cca3a85247ee9ebbc30e62cca1
